package com.rinnsan.creavity.core.theme

import androidx.compose.ui.graphics.Color

// --- RINNSAN CREAVITY PALETTE ---
// "The Silent System"

val VoidBlack = Color(0xFF050505)      // Nền chính (Vũ trụ)
val TeslaWhite = Color(0xFFFFFFFF)     // Chữ tiêu đề (Đèn LED xe)
val CyberAcid = Color(0xFFCCFF00)      // Điểm nhấn (Năng lượng hạt nhân)
val PhantomGrey = Color(0xFF1F1F1F)    // Khối phụ (Bóng ma)
val GlitchRed = Color(0xFFFF003C)      // Màu lỗi (Khi có Glitch)
val TechSilver = Color(0xFF888888)     // silver
val SystemError = Color(0xFFFF2E2E)

val DarkOverlay = Color(0x99000000)    // Lớp phủ mờ
val GridLineColor = Color(0xFF333333)  // Màu đường kẻ lưới kỹ thuật